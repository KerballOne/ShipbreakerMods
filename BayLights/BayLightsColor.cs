using UnityEngine;

/// Mirror of the Unity-side BayLightsColor component (ShipbreakerShipbuilder/Assets/Scripts/BayLightsColor.cs).
/// Must match exactly: same class name, no namespace, same public field name and type.
public class BayLightsColor : MonoBehaviour
{
    public Color color = Color.white;
}
